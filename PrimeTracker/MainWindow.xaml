<Window x:Class="PrimeTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrimeTracker"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <GroupBox Header="Movies">
            <ListBox ItemsSource="{Binding ExpiredMovies}" DisplayMemberPath="Title">
            </ListBox>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Shows">
            <ListBox ItemsSource="{Binding ExpiredShows}" DisplayMemberPath="Title">
            </ListBox>
        </GroupBox>
    </Grid>
</Window>
